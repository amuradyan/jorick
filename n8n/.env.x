# Placeholder for env vars the n8n workflow consumes.
#
# Most credentials (Anthropic API key, etc.) are set inside n8n's UI as
# "credentials" attached to the relevant node — those don't flow through env.
# This file is the spot for any process-level env the workflow ends up needing.
